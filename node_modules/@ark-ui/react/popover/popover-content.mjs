'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { splitPresenceProps } from '../presence/split-presence-props.mjs';
import { usePopoverContext } from './popover-context.mjs';
import { PopoverPresence } from './popover-presence.mjs';

const PopoverContent = forwardRef((props, ref) => {
  const [presenceProps, popoverContentProps] = splitPresenceProps(props);
  return /* @__PURE__ */ jsx(PopoverPresence, { ...presenceProps, children: /* @__PURE__ */ jsx(InnerPopoverContent, { ref, ...popoverContentProps }) });
});
const InnerPopoverContent = forwardRef((props, ref) => {
  const { contentProps } = usePopoverContext();
  const mergedProps = mergeProps(contentProps, props);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
});

export { PopoverContent };
