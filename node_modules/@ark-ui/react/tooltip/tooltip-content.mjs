'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { splitPresenceProps } from '../presence/split-presence-props.mjs';
import { useTooltipContext } from './tooltip-context.mjs';
import { TooltipPresence } from './tooltip-presence.mjs';

const TooltipContent = forwardRef((props, ref) => {
  const [presenceProps, tooltipContentProps] = splitPresenceProps(props);
  return /* @__PURE__ */ jsx(TooltipPresence, { ...presenceProps, children: /* @__PURE__ */ jsx(InnerTooltipContent, { ref, ...tooltipContentProps }) });
});
const InnerTooltipContent = forwardRef((props, ref) => {
  const { contentProps } = useTooltipContext();
  const mergedProps = mergeProps(contentProps, props);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
});

export { TooltipContent };
