'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { splitPresenceProps } from '../presence/split-presence-props.mjs';
import { useHoverCardContext } from './hover-card-context.mjs';
import { HoverCardPresence } from './hover-card-presence.mjs';

const HoverCardContent = forwardRef((props, ref) => {
  const [presenceProps, hoverCardContentProps] = splitPresenceProps(props);
  return /* @__PURE__ */ jsx(HoverCardPresence, { ...presenceProps, children: /* @__PURE__ */ jsx(InnerHoverCardContent, { ref, ...hoverCardContentProps }) });
});
const InnerHoverCardContent = forwardRef((props, ref) => {
  const { contentProps } = useHoverCardContext();
  const mergedProps = mergeProps(contentProps, props);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
});

export { HoverCardContent };
