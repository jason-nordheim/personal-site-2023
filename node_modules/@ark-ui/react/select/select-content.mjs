'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { splitPresenceProps } from '../presence/split-presence-props.mjs';
import { useSelectContext } from './select-context.mjs';
import { SelectPresence } from './select-presence.mjs';

const SelectContent = forwardRef((props, ref) => {
  const [presenceProps, selectContentProps] = splitPresenceProps(props);
  return /* @__PURE__ */ jsx(SelectPresence, { ...presenceProps, children: /* @__PURE__ */ jsx(InnerSelectContent, { ref, ...selectContentProps }) });
});
const InnerSelectContent = forwardRef((props, ref) => {
  const { contentProps } = useSelectContext();
  const mergedProps = mergeProps(contentProps, props);
  return /* @__PURE__ */ jsx(ark.ul, { ...mergedProps, ref });
});

export { SelectContent };
