'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { splitPresenceProps } from '../presence/split-presence-props.mjs';
import { useDialogContext } from './dialog-context.mjs';
import { DialogPresence } from './dialog-presence.mjs';

const DialogContent = forwardRef((props, ref) => {
  const [presenceProps, dialogContentProps] = splitPresenceProps(props);
  return /* @__PURE__ */ jsx(DialogPresence, { ...presenceProps, children: /* @__PURE__ */ jsx(InnerDialogContent, { ref, ...dialogContentProps }) });
});
const InnerDialogContent = forwardRef((props, ref) => {
  const { contentProps } = useDialogContext();
  const mergedProps = mergeProps(contentProps, props);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
});

export { DialogContent };
