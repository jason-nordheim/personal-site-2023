'use client';
import { jsx } from 'react/jsx-runtime';
import { mergeProps } from '@zag-js/react';
import { ark } from '../factory.mjs';
import { forwardRef } from '../forward-ref.mjs';
import { useDialogContext } from './dialog-context.mjs';
import { DialogPresence } from './dialog-presence.mjs';

const DialogBackdrop = forwardRef((props, ref) => /* @__PURE__ */ jsx(DialogPresence, { children: /* @__PURE__ */ jsx(InnerDialogBackdrop, { ref, ...props }) }));
const InnerDialogBackdrop = forwardRef((props, ref) => {
  const { backdropProps } = useDialogContext();
  const mergedProps = mergeProps(backdropProps, props);
  return /* @__PURE__ */ jsx(ark.div, { ...mergedProps, ref });
});

export { DialogBackdrop };
